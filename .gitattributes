*.ipynb filter=nbstripout
*.zpln filter=nbstripout
*.ipynb diff=ipynb

# No exemptions: every notebook is stripped of outputs before commit/push.
# Step results are kept as PNGs in per-notebook result_<notebook>/ folders instead.
